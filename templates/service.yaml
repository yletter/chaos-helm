apiVersion: v1
kind: Service
metadata:
  name: {{ include "chaos-helm.fullname" . }}
  labels:
    {{- include "chaos-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chaos-helm.selectorLabels" . | nindent 4 }}
